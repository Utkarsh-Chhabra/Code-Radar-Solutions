#include <stdio.h>
int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if(a>=20 && b=1){
        print("Eligible");
    }
    else{
        printf("Not Eligible");
    }

    return 0;
}